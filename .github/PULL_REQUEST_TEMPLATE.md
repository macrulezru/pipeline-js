## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] `npm run test:types` passes (if you touched `pipe()`/builder types)
- [ ] `npm run test:coverage` passes thresholds (no lowered thresholds)
- [ ] `npm run size` passes (no unexplained bundle-size growth)
- [ ] Added/updated tests in `tests/` for the new behavior
- [ ] Added a `CHANGELOG.md` entry under `[Unreleased]`
- [ ] If breaking: documented old vs. new behavior and migration path

## Breaking change?

<!-- Yes/No. If yes, summarize the migration for consumers. -->
